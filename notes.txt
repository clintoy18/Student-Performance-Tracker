command to update dotnet ef:
dotnet tool update --global dotnet-ef

run migrations:
1. cd backend/ASI.Basecode.WebApp
2. dotnet ef migrations add <MigrationName> --project ../ASI.Basecode.Data
3. dotnet ef database update

IF MIGRATIONS DONT WORK!
=It might be that dotnet-ef version does not match the project's: 9.0.6.
-To check version, do:
1. dotnet-ef --version
-To match version, do:
1. dotnet tool uninstall --global dotnet-ef
2. dotnet tool install --global dotnet-ef --version 9.0.6

=> Make sure you're migrating at the right folder! ASI.Basecode.WebApp

Commands:
dotnet add package Microsoft.EntityFrameworkCore.Sqlite --version 9.0.6
dotnet remove package Microsoft.EntityFrameworkCore.SqlServer